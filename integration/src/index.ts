import { Doctor } from "@hospital-sdk-trimmtrab/doctor";
import { Patient } from "@hospital-sdk-trimmtrab/patient";
import { Scheduler } from "@hospital-sdk-trimmtrab/scheduler";

console.log(new Doctor());
console.log(new Scheduler());
console.log(new Patient());
